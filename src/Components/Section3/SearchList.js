import React from "react";
import classes from './Section3.module.css';


const SearchList = (props) => {
    return ( "NOthing" )
}
        // <ul> {props.quotes.map((quote))}
        //     <span className={classes.name}>${place.quote}</span>
        // </ul>
export default SearchList;